# Network Accessible Links Rule

Whenever starting, running, or reporting on running applications or services:
- Always bind the services to host `0.0.0.0` or enable host exposure.
- Always provide the local network IP URL (e.g., `http://<LAN-IP>:<PORT>`) along with localhost so the user can easily share accessible links with their team across the local network.
